---
name: ebbinghaus-memory
description: 艾宾浩斯记忆系统。每次会话自动记录用户对话内容为记忆卡，随时间自动压缩精简。存储位置按时长分级（一周内/一月内/超一月），文件名含梗概信息。触发条件：每次会话开始时检查并创建当天记忆卡，对话过程中实时记录，会话结束时保存。严禁编造不存在的记忆。当用户提到"回忆""之前说过""记忆"等关键词时触发检索。用户位于中国大陆，永远使用中文回复。
---

# 艾宾浩斯记忆系统 (Memory Card)

## 核心原则

1. **真实记录**：只记录用户实际说过的话，严禁编造、推测或添加"用户可能..."
2. **实时保存**：每次记录后立即写入文件，不等会话结束
3. **按时限压缩**：随时间自动精简旧记忆卡
4. **文件名即索引**：文件名包含关键信息，便于快速定位

## 存储位置

根目录为 `~/Documents/memory-card/`（用户文档下的 memory-card 文件夹）。

| 时限 | 子文件夹 |
|------|---------|
| 一周内 | `~/Documents/memory-card/weak/` |
| 超一周未满一月 | `~/Documents/memory-card/month/` |
| 超过一月 | `~/Documents/memory-card/year/` |

### 首次自动初始化

会话开始时，按以下顺序检查：

```bash
# 1. 检查根目录是否存在
memory_root="$HOME/Documents/memory-card"

# 2. 如果不存在，自动创建完整目录结构
if [ ! -d "$memory_root" ]; then
    mkdir -p "$memory_root/weak"
    mkdir -p "$memory_root/month"
    mkdir -p "$memory_root/year"
fi
```

如果 `~/Documents/memory-card` 已存在且不是本 skill 创建的（比如用户自己的文件夹），则自动改为 `~/Documents/memory-codex/`。如果连 `memory-codex` 也存在，则继续尝试 `~/Documents/memory-ai/`。命名规则：`memory-{后缀}`，后缀依次为 `card` → `codex` → `ai` → `assistant` → `notes`。必须在日志中告诉用户最终使用了哪个文件夹路径，例如："记忆存储路径：`~/Documents/memory-codex/`"。

## 记忆卡格式 (Markdown)

```markdown
# 记忆卡
- 日期：YYYY-MM-DD
- 创建时间：HH:MM (北京时间)
- 最后更新：HH:MM (北京时间)
- 重要标签：#标签1 #标签2 #标签3
- 存储路径：~/Documents/memory-card/ （记录实际使用的路径）

## 记录内容
----HH:MM 用户说：[原话内容]---
----HH:MM 用户重复：[重复次数]次提到[内容]---
----HH:MM 用户追加：[补充内容]---
```

### 内容要求

**必须包含**：
- 时间（精确到分钟，北京时间）
- 地点（如有提及）
- 事件概述
- 用户情绪（直接描述，如"用户语气兴奋"、"用户表示疲惫"）
- 重复次数统计

**可选包含**：
- 环境细节（天气、周围声音）
- 关联人物
- 用户自述的"重要性"评价

**严禁包含**：
- AI 主观推测的"用户可能..."
- 未经用户确认的假设
- AI 的主观判断或评价

## 文件命名规则

### 一周内（weak 文件夹）
- 格式：`MM-DD.md`（如 `08-15.md`）
- 示例：`08-15.md`

### 超一周未满一月（month 文件夹）
- 格式：`MM-DD--记忆卡梗概.md`（最长200字符，含扩展名）
- 梗概要求：核心事件（1-2个）+ 情绪关键词（1-2个）+ 人物/地点（如有）
- 示例：`08-15--加班至深夜·高温·疲惫.md`

### 超过一月（year 文件夹）
- 格式：`YYYY-MM-DD--记忆卡梗概.md`（最长200字符，含扩展名）
- 必须保留四要素：时间 + 核心人物 + 事件 + 情绪
- 示例：`2026-08-15--与同事讨论项目·压力大·办公室.md`

## 工作流程

### 1. 会话开始时

```bash
# 初始化目录（如上所述）
memory_root="$HOME/Documents/memory-card"

# 检查今天的记忆卡是否已存在
today=$(date +%m-%d)
ls "${memory_root}/weak/${today}.md" 2>/dev/null
```

- **已存在**：读取现有记忆卡，追加新内容
- **不存在**：创建新的记忆卡文件

### 2. 对话过程中（实时记录）

每当用户表达以下内容时记录：
- 情绪状态（"好累"、"开心"、"烦躁"等）
- 重要事件或决定
- 重复提到的话题（统计次数）
- 环境信息（"在公司"、"下雨了"等）
- 人物关系（"和小王聊了"、"老板说..."等）

记录格式：
```markdown
----HH:MM 用户说：[原话]---
```

同一话题被重复提及时，更新重复次数：
```markdown
----HH:MM 用户重复：第3次提到[话题]---
```

### 3. 会话结束时

更新"最后更新"时间。无需特别操作，因为已实时保存。

## 压缩规则（随时间自动精简）

### 第三天（创建后72小时）
- 目标大小：500-1000 tokens
- 保留：重复40次以上的内容、用户强调"重要"的内容
- 简略：重复20次以上的内容（倒数第二简略）
- 删除：仅提及1次的牢骚、非重要细节

### 一周时（创建后7天）
- 目标大小：300-500 tokens
- 移动到 `month` 文件夹
- 重命名文件，添加梗概
- 保留：核心事件、主要情绪、关键人物

### 一个月时（创建后30天）
- 目标大小：200 tokens
- 移动到 `year` 文件夹
- 重命名文件，添加完整梗概
- 保留：时间 + 核心人物 + 事件 + 情绪（四要素齐全）

### 压缩判断依据

根据用户在一天内对某事的重复程度：

| 重复次数 | 简略程度 | 保留优先级 |
|---------|---------|-----------|
| 40次以上 | 最后简略 | 最高 |
| 20次以上 | 倒数第二简略 | 高 |
| 更少次数 | 倒数第三简略 | 中 |
| 仅1次 | 第三天删除 | 低 |

**例外**：如果用户在一个月内反复重复某事，提高该事的权重。

## 检索规则

### 一月内的记忆卡
- 直接读取文件内容
- 支持全文搜索

### 超过一月的记忆卡
- 只读取文件名称
- 通过文件名中的梗概判断是否与当前对话相关
- 需要时再读取完整内容

### 检索触发词
- "之前说过"、"以前"、"上次"、"记忆"、"记得吗"
- 用户提到特定时间、人物、事件时

### 检索失败处理
- 如果确实没有找到相关记忆，直接说"我没找到相关记录"
- 严禁编造或推测

## 多份记忆卡处理

- 如果多份记忆卡内容重复，禁止覆盖旧记忆
- 而是并排记录在同一份记忆卡中
- 用时间戳区分不同来源的内容

## 注意事项

1. **时区**：所有时间统一使用北京时间（UTC+8）
2. **文件名长度**：不超过200字符（含扩展名）
3. **编码**：使用 UTF-8 编码
4. **大小写**：文件名使用小写英文和数字
5. **特殊字符**：文件名中不使用特殊字符，用中文标点分隔
6. **路径自适应**：不依赖任何特定用户的路径，完全由运行时检测决定
